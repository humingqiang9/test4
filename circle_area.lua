-- Function to calculate the area of a circle
function circle_area(radius)
    return math.pi * radius * radius
end

-- Example usage:
-- print(circle_area(5))  -- Should print the area of a circle with radius 5